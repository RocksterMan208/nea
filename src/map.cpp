#include "map.hpp"

Map::Map(float tileSize,int tileMap, Color tileSolid, Color tileEmpty)
{
    float size = tileSize;
    int mapGrid = tileMap;
    Color full = tileSolid;
    Color empty = tileEmpty;
}

void Map::DrawMap()
{
    std::cout << "Hello World!" << std::endl;
}